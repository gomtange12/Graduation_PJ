todo
<소현>
모델공유 문제: 본트랜스폼행렬을 게임오브젝트가 가져야한다. 그래서 필요한 정보를 구조체로 뽑아 파일에서 로드할 때 불러야하는데  frame정보를 잘 받지 못했음. 
그래서 특강을 듣고 그 수업내용 토대로 다시 바꿔보고자함. --> 796 해결

<천기>
- 무기 렌더링 문제점 : 무기를 렌더링 할 때 빤짝거리듯 렌더링이 잘못되는 결과
-> 애니메이션의 문제 : 무기 렌더의 문제가 아닌 애니메이션의 문제로 인해 반짝거리는 듯한 문제가 발생

01/22
움직이는 slash 애니메이션 관련
다렉에서 설정값을 바꿔봤는데 현재 우리의 모델이 모델이 애니메이션 되는 것이 아니라 더미를 통해 모델이 따라가는 것을 확인하고 현재 모델에서 지금 애니메이션을 무조건 써야하는 것은 아니기 떄문에 제자리 애니메이션을 활용하는것을 우선의 해결 과제로 잡고 제자리 애니메이션을 통한 걷기,뛰기, 대쉬, 점프 모션등을 애니메이션 블렌딩/믹싱을 통해 자연스럽게 하자! 
더미를 따라가 모델의 위치가 제대로 잡히지 않는것은 여전히 해결과제이므로 후에 이러한 문제로 또 문제가 발생했을 경우엔
이제까지 시도한 콜백함수에서 모델의 포지션을 재지정해주는 것이 아니라 모델의 루트모션문제로 접근 / 루트 오브젝트의 위치를 재지정하는 방향으로 가볼것

제자리 애니메이션
1. 기본애니메이션 셋트 준비

- 걷기 -애니메이션되고 작아짐 
- 뛰기 - 애니메이션되고 작아짐 /디버그로 돌리면 애니메이션 x
- 점프 ㅇ
- 대쉬 - 파일 아예 안열림(소현만)

문제발생-
기타와 스킨 제대로 입힌 미라 디폴트를 애니메이션파일을 추출해 다렉에서 돌렸을 때 애니메이션별로 달랐음
오늘 모델을수정했기 때문에 모델 익스포트 방식 다시 수정해서 (embed media) 다시 익스포트 후 내일부터 다시 확인할것
현재 문제점: 디버그에서는 애니메이션이 정상사이즈로 한번 실행된 후 크기가 정말 1/100크기로 작아짐. 스케일 설정은 따로 이전과 변함 없음(유니티, 다렉 모두)

의심1. 일단 오늘 모델을 바꿨으므로 모델을 마저 수정
2. 애니메이션의 호환성으로 인해 맞지 않을 수도 있음 --> 확인중
3. 크기가 급격히 작아지는것은 애니메이션 프레임변화에 따른 차이를 의심. 예를들어 이전까지는 모두 60~100이었는데 지금 모두 걷기 뛰기임

MAX로 작업한 모델을 Unity로 애니메이션과 모델정보를 추출후 DX12에서  렌더시 오른쪽 팔전체가 반짝거리는 문제가 발생하였다.

의심1.애니메이션의 문제 -> 기존 교수님의 프로젝트안에 있는 PLAYER.FBX를 사용하여 같은방법으로 애니메이션 추출시 이상없음.
2. MAX설정의문제 및 구조의 문제 -> 문제 없었음
3. 모델 리깅할때 더미가 많은데 더미 때문 -> 더미를 없애도 문제없었음
결과 의심 : 바이패드와 본 의 차이 (리깅의 문제)
 

01/24까지 작업결과 구매한 캐릭터에 머리뼈가 없어 애니메이션과 호환 X 더미 따라가는 문제 수정한후에는 애니메이션이 입혀지지 않음
--> 리깅만 주변 지인에게 외주


